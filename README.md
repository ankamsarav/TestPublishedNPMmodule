## Consumed the npm module which I published here
https://www.npmjs.com/package/easyshadows

## To run the project    
    ```
    npm install
    ```

## To build the project

    Install Parcel 
    ```
    npm i -g parcel
    parcel index.html
    ```    
    